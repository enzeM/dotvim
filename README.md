# dotvim

```
#Step 1: clone repo:
git clone http://github.com/enzeM/dotvim.git ~/.vim

#Setp 2: hard link the config files
ln -s ~/.vim/vimrc ~/.vimrc
ln -s ~/.vim/bash_profile ~/.bash_profile

#Step 3: download vim plug-in
cd ~/.vim
git submodule init
git submodule update
```
